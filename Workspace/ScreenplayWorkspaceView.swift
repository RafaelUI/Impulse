import SwiftUI

struct ScreenplayWorkspace: View {
    let project: WritingProject
    
    var body: some View {
        Text("Сценарий — в разработке")
            .font(.title2)
            .foregroundStyle(.secondary)
    }
}
